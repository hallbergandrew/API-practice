<h1>Messages</h1>

<ul>
    <% @messages.each do |message| %>
      <li><%= link_to message.id, message_path(message) %></li>
    <% end %>
</ul>

<%= link_to "New Message", new_message_path %>

<%= link_to "Add to Address Book", new_addressbook_path %>
